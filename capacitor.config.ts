import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cargo.manager',
  appName: 'Cargo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
